import { ThemeButton } from '@components/ThemeButton/ThemeButton';

export const PageHeader = () => {
  return (
    <div className="page header wrapper">
      <div className="text-xl font-semibold text-center text-primary-500">./DASHBOARD</div>
      <ThemeButton />
    </div>
  );
};
